import SectionPage, { sectionMetadata } from "@/components/SectionPage";

export const metadata = sectionMetadata("serie-tv");

export default function SerieTvPage() {
  return <SectionPage slug="serie-tv" />;
}
