import SectionPage, { sectionMetadata } from "@/components/SectionPage";

export const metadata = sectionMetadata("dc-universe");

export default function DcUniversePage() {
  return <SectionPage slug="dc-universe" />;
}
